# API keys for chatbot demos
# Copy this file to .env and fill in your keys.
#
# IMPORTANT: Never commit .env files with real keys.
# The .env file is already in .gitignore.

# Anthropic (Claude)
# 1. Create an account at https://console.anthropic.com
# 2. Go to Settings > API Keys
# 3. Click "Create Key" and copy the value
# 4. Paste it below (replacing "your-key-here")
# Note: Anthropic requires a credit card, even for the free tier.
ANTHROPIC_API_KEY=your-key-here

# HuggingFace
# 1. Create an account at https://huggingface.co
# 2. Go to Settings > Access Tokens
# 3. Click "New token", select "Read" scope, and copy the value
# 4. Paste it below (replacing "your-token-here")
# Note: A token enables faster downloads and access to gated models.
HF_TOKEN=your-token-here

# llama.cpp chatbot configuration
# For local testing, leave these commented out (defaults to localhost:8502 with "dummy" key)
# For remote servers, uncomment and set your server URL and API key
# PERDRIZET_URL=your-server-url-here
# PERDRIZET_API_KEY=your-api-key-here

# PostgreSQL + pgvector (used by the RAG demo - demos/rag_system/rag_demo.py)
DB_USER=user
DB_PASSWORD=password
DB_HOST=host
DB_PORT=5432
DB_NAME=database
